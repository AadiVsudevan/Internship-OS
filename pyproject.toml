[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
pythonpath = ["."]
# Suppress dateparser's noisy locale warnings during tests
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::UserWarning",
]
